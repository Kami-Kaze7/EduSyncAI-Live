<UserControl x:Class="EduSyncAI.MyEnrolledCoursesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EduSyncAI">
    
    <Grid Background="#ECF0F1">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30">
                <TextBlock Text="📚 My Enrolled Courses" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>
                
                <TextBlock x:Name="StatusText" Text="Loading your courses..." Foreground="#7F8C8D" Margin="0,0,0,20"/>
                
                <ItemsControl x:Name="EnrolledCoursesItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" Padding="20" Margin="0,0,0,15" CornerRadius="5" BorderBrush="#BDC3C7" BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding CourseTitle}" FontSize="18" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding CourseCode}" Foreground="#7F8C8D" Margin="0,5,0,0"/>
                                    </StackPanel>

                                    <Button Grid.Column="1" Content="❌ Unenroll" Tag="{Binding CourseId}"
                                            Click="Unenroll_Click"
                                            Background="#E74C3C" Foreground="White" Padding="20,10" FontWeight="Bold"
                                            BorderThickness="0" Cursor="Hand" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                
                <!-- Empty State -->
                <StackPanel x:Name="EmptyState" Visibility="Collapsed" HorizontalAlignment="Center" Margin="0,50,0,0">
                    <TextBlock Text="📭" FontSize="72" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    <TextBlock Text="You haven't enrolled in any courses yet" FontSize="18" Foreground="#7F8C8D" HorizontalAlignment="Center"/>
                    <TextBlock Text="Go to 'Browse Courses' to enroll" FontSize="14" Foreground="#95A5A6" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
