<UserControl x:Class="EduSyncAI.CourseEnrollmentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EduSyncAI">
    
    <Grid Background="#ECF0F1">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30">
                <TextBlock Text="📚 Available Courses" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>
                
                <TextBlock x:Name="StatusText" Text="Loading courses..." Foreground="#7F8C8D" Margin="0,0,0,20"/>
                
                <ItemsControl x:Name="CoursesItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" Padding="20" Margin="0,0,0,15" CornerRadius="5" BorderBrush="#BDC3C7" BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding CourseTitle}" FontSize="18" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding CourseCode}" Foreground="#7F8C8D" Margin="0,5,0,0"/>
                                    </StackPanel>

                                    <Button Grid.Column="1" Content="{Binding ButtonText}" Tag="{Binding CourseId}"
                                            Click="EnrollButton_Click"
                                            Background="{Binding ButtonColor}" Foreground="White" Padding="20,10" FontWeight="Bold"
                                            BorderThickness="0" Cursor="Hand" VerticalAlignment="Center"
                                            IsEnabled="{Binding CanEnroll}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
